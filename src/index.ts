import webpack from 'webpack';
import build from './build';
import clean from './clean';
import server from './server';
import { DoolConfig } from './config';

export {
  build,
  clean,
  server,
  DoolConfig,
  webpack
};
